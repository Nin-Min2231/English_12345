org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Workaround (2026-07-23): Kotlin incremental compiler daemon fails to close
# its cache files on this Windows machine ("Could not close incremental
# caches") when compiling record_android/shared_preferences_android — known
# Kotlin/Windows file-locking issue. Disabling incremental compilation avoids
# it; only affects build speed, not app behavior.
kotlin.incremental=false
